<section className="cta-section">
    <div className="cta-content">
        <h2 className="cta-title">
            Get Premium Metal Cutting Wheels
        </h2>

        <p className="cta-description">
            Purchase Junctions is a genuine Metal Cutting Wheel manufacturers, suppliers,
            dealers, distributors, stockist, exporters in India.
            Contact us today for more information!
        </p>

        <div className="cta-actions">
            <button className="cta-button cta-button--primary">
                Request Quote
            </button>
            <button className="cta-button cta-button--secondary">
                Contact Sales
            </button>
        </div>
    </div>
</section>







//css
/* ============================
   Call To Action Section
============================ */

.cta-section {
    background: linear-gradient(135deg, #2d3748 0%, #1a202c 100%);
    color: #ffffff;
    padding: 96px 0;
    border-radius: 32px 32px 0 0;
    margin-top: 72px;
    text-align: center;
    position: relative;
    overflow: hidden;

    /* Fade-in animation */
    animation: cta-fade-in-up 0.7s ease-out both;
}

.cta-section::before {
    content: '';
    position: absolute;
    inset: 0;
    background: url("data:image/svg+xml,%3Csvg width='60' height='60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='%23ffffff' fill-opacity='0.05'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4z'/%3E%3C/g%3E%3C/svg%3E");
}

.cta-content {
    max-width: 880px;
    margin: 0 auto;
    padding: 0 24px;
    position: relative;
    z-index: 1;
}

.cta-title {
    font-size: 2.75rem;
    font-weight: 800;
    margin-bottom: 32px;
    position: relative;
}

.cta-title::after {
    content: '';
    position: absolute;
    bottom: -12px;
    left: 50%;
    transform: translateX(-50%);
    width: 120px;
    height: 4px;
    background: linear-gradient(90deg, #38a169, #2f855a);
    border-radius: 2px;
}

.cta-description {
    font-size: 1.2rem;
    line-height: 1.8;
    margin-bottom: 48px;
    color: rgba(255, 255, 255, 0.9);
}

/* CTA Actions */
.cta-actions {
    display: flex;
    gap: 24px;
    justify-content: center;
    flex-wrap: wrap;
}

.cta-button {
    padding: 20px 48px;
    font-size: 1.15rem;
    font-weight: 700;
    border-radius: 50px;
    border: none;
    cursor: pointer;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    position: relative;
    overflow: hidden;
    transition: all 0.35s cubic-bezier(0.4, 0, 0.2, 1);
}

/* Ripple animation */
.cta-button::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    width: 0;
    height: 0;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.25);
    transform: translate(-50%, -50%);
    transition: width 0.6s ease, height 0.6s ease;
}

.cta-button:hover::before {
    width: 300px;
    height: 300px;
}

.cta-button--primary {
    background: linear-gradient(135deg, #38a169, #2f855a);
    color: #ffffff;
    box-shadow: 0 10px 30px rgba(56, 161, 105, 0.3);
}

.cta-button--primary:hover {
    transform: translateY(-4px);
    box-shadow: 0 16px 40px rgba(56, 161, 105, 0.4);
}

.cta-button--secondary {
    background: transparent;
    color: #ffffff;
    border: 2px solid #ffffff;
}

.cta-button--secondary:hover {
    background: #ffffff;
    color: #2d3748;
    transform: translateY(-4px);
    box-shadow: 0 16px 40px rgba(255, 255, 255, 0.2);
}

/* Responsive */
@media (max-width: 768px) {
    .cta-title {
        font-size: 2rem;
    }

    .cta-actions {
        flex-direction: column;
        align-items: center;
    }

    .cta-button {
        width: 100%;
        max-width: 320px;
    }
}

/* Animation Keyframes */
@keyframes cta-fade-in-up {
    from {
        opacity: 0;
        transform: translateY(30px);
    }

    to {
        opacity: 1;
        transform: translateY(0);
    }
}