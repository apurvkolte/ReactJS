docker-compose up --build

docker compose --progress=plain up --build

test
docker compose version

docker compose config

docker compose build --no-cache

delet rebuild
docker compose down -v   # -v deletes volume so MySQL restarts fresh
docker compose up --build

%APPDATA%\Docker  delete all

RUN npm install immutable@3 --legacy-peer-deps
RUN npm run build
docker compose down -v
docker compose up --build



sttaus
docker ps -a



docker-compose restart app     
docker cp sgsro.sql sgsrodep-db-1:/sgsro.sql
docker ps -a     
docker logs sgsrodep-db-1         
docker start sgsrodep-db-1    
docker exec -it sgsrodep-db-1 bash
mysql -u root -proot sgsro < /sgsro.sql




get backup in MySQL 
docker cp backup.sql sgsrodep-db-1:/backup.sql
docker exec sgsrodep-db-1 mysqldump -u root -proot sgsro > backup.sql


upload file backup
docker cp ./uploads-backup sgsrodep-app-1:/app/public/uploads


1.docker exec sgsrodep-db-1 mysqldump -u root -proot sgsro > backup.sql
2.docker cp sgsrodep-app-1:/app/public/uploads ./uploads-backup


log error
docker logs sgsrodep-app-1

docker logs -f sgsrodep-app-1

//resatr
docker-compose restart app